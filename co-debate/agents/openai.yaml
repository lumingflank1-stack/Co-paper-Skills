interface:
  display_name: "Co-Debate"
  short_description: "Debate five hypotheses for one topic"
  default_prompt: "Use $co-debate as the final $co-mimic-managed subskill to challenge and rank exactly five hypotheses, then return the selected hypothesis to $co-mimic."
