interface:
  display_name: "Co-Plan"
  short_description: "Plan a module with discovery and validation"
  default_prompt: "Use $co-plan only after I select one $co-debate hypothesis; design its module relationship, discovery layer, validation layer, rescue, data plan, and decision rules."
