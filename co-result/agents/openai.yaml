interface:
  display_name: "Co-Result"
  short_description: "Write layered module Results"
  default_prompt: "Use $co-result to draft Chinese module Results with node-discovery evidence, validation evidence, rescue when needed, figure prompts, raw-data needs, and simulated Excel data when explicitly requested."
